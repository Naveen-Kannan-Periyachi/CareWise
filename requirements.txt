# CareWise Bio - Requirements

# Core dependencies
# CareWise - Unified Health Research Assistant
# 
# This file is for the OLD project structure.
# For the NEW unified project, use: carewise/requirements.txt
#
# To install dependencies for the unified project:
#   cd carewise
#   pip install -r requirements.txt

# Core dependencies (legacy)
requests>=2.31.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.4.0>=2.31.0

# Backend API server
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Data processing (if needed later)
# pandas>=2.0.0
# numpy>=1.24.0

# For local LLM (Ollama - external)
# No Python package needed, Ollama runs as separate service

# Development/Testing
# pytest>=7.4.0
